# gto-poker-async-duel — promoted from AutoBuilder

> Auto-generated by AutoBuilder's `.github/workflows/completion-triggered-fork.yml` at the moment this repository was forked. You are free to edit this file — AutoBuilder will not re-write it.

This repository is a standalone fork of AutoBuilder run `gto-poker-async-duel`, promoted to its own product life on **2026-05-16T21:06:14Z** after the user ratified the initial delivery as installable + accessible.

## You are here

You are a Claude Code session running against the **product life** of this artifact. The build life is upstream in [the AutoBuilder corpus](https://github.com/mondrianaire/auto-builder) and ended at promotion. The product life starts here.

**What this means in practice:**

- You can edit any file in this repository. AutoBuilder does not write back here.
- There is **no Convergence Verifier** watching your changes. The build factory's safety nets ended at promotion. You are responsible for testing, validating, and not regressing things.
- The AutoBuilder corpus entry at `runs/gto-poker-async-duel/` in the upstream repo is **frozen at the promotion commit**. It will not change. If you find a flaw in the build's decisions, fix it here — do not try to amend the corpus.
- If the user re-prompts AutoBuilder with this same prompt verbatim, that produces a *new* fork at a different repo (likely `mondrianaire/gto-poker-async-duel-AB-2` or similar). It does NOT overwrite this one. Each AutoBuilder build is a distinct corpus measurement; this repo is its own product trajectory.

## About AutoBuilder (brief)

AutoBuilder is an autonomous build system. The user gives it a single prompt describing what they want; it builds a deployable application end-to-end without further interaction during the build. The build's outcome — success, partial success, recoverable failure — becomes a measurement record in a corpus of past builds.

The North Star is: **always deliver a working artifact**. Gaps in understanding are documented in the build's uncertainty manifest, not used as escape hatches. The user always gets something they can run.

A build moves through Phase 1 (Initial Delivery — end-to-end run) and optionally Phase 2 (In Limbo — strictly-rectification commits if the Phase-1 artifact was broken). When all three completion gates are green — install instructions clear, deliverable accessible, internal verification passing — the build is RATIFIED and its corpus entry freezes. Some ratified builds are then PROMOTED (opt-in user decision) to a standalone repo for product life. That's how this repo came to exist.

For the full ethos (recommended if you'll be making non-trivial decisions): see [architecture/autobuilder-ethos.md](https://github.com/mondrianaire/auto-builder/blob/main/architecture/autobuilder-ethos.md) in the upstream corpus.

## About this build

| Field | Value |
|---|---|
| Slug | `gto-poker-async-duel` |
| Deliverable kind | `web_app` |
| Verification verdict | `pass_with_concerns` |
| Live URL | https://mondrianaire.github.io/gto-poker-async-duel-AB/ |
| Ratified | 2026-05-16T10:17:18.869Z by Jett |
| Architecture version at build | `unknown` |

**Original prompt** (the entire user input that produced this build — read this first if you want to know what was being asked for):

> Previously you created an application called "GTO Poker Training" that was a proof of concept and a mini research and analysis project for creating a web application that allows a user to play poker, track useful GTO orientated poker stats and had an almost "quiz like" application relating to presenting various "edge cases" that highlight specific GTO philosophies and presenting the user with various choices and ranking them and providing feedback based on interpretation of GTO research.

I introduced my mom to this application and she loved it. We would read through the hand descriptions, and guess to each other what the correct answer was. When we entered it and saw the actual data, it was so much fun and so informative to be able to read the GTO description and defense of "optimal" plays while identifying plays where confidence may be lower and the answer may not be as clear cut.

I do not live physically near my mom and I was thinking how easy it would be to extrapolate on this GTO build and attempt to create an asynchronous multiplayer GTO Poker head to head quiz game where users are presented with identical GTO "gotchas" and there is some type of communication either direct or in game to discuss or diagree with the GTO verified action.

In addition to a selection of a correct answer, it would be neat to implement a "confidence" function where users indicate how sure they are of their decisions. This would allow a post-game wrap up screen that highlighted the GTO gotchas that showed the highest confidence gap between differing answers.

It is imperative that this application fully can be hosted on github pages, that the game fully functions asynchronsly, and users can "build up" a handful of answers before they must wait for the other user to submit their answers and then create more for the opposing player. This rotation goes for a set number of rounds and then statistics regarding player performance and player agreement is shown to both users. If possible implement an opt in notification function that will use mobile or desktop notification libraries to notify the opposing player that it is their turn.

## The build's history (corpus pointers)

You don't *need* to read these to work in this repo — the product life is its own thing. But when you find yourself asking *"why was this decision made this way"* or *"what did the build process consider and reject"*, these are where to look. Everything below lives in the AutoBuilder repo, not this one.

| Resource | What's in it | When you'd want it |
|---|---|---|
| [`runs/gto-poker-async-duel/PROJECT-OVERVIEW.md`](https://github.com/mondrianaire/auto-builder/blob/main/runs/gto-poker-async-duel/PROJECT-OVERVIEW.md) | Cat 1 wrap-up — plain-English orientation for fresh readers | First stop for non-AutoBuilder-vocabulary context |
| [`runs/gto-poker-async-duel/run-report.md`](https://github.com/mondrianaire/auto-builder/blob/main/runs/gto-poker-async-duel/run-report.md) | Cat 2 build narrative — written in AutoBuilder vocabulary | Understanding the build phase-by-phase |
| [`runs/gto-poker-async-duel/decisions/`](https://github.com/mondrianaire/auto-builder/tree/main/runs/gto-poker-async-duel/decisions) | Per-role decision ledgers (Discovery, Technical Design, etc.) | "Why did the build choose React over Vue" type questions |
| [`runs/gto-poker-async-duel/audit/`](https://github.com/mondrianaire/auto-builder/tree/main/runs/gto-poker-async-duel/audit) | Per-role audit logs (full role transcripts) | Deep forensics on what each role actually did |
| [`runs/gto-poker-async-duel/output/verification/report.json`](https://github.com/mondrianaire/auto-builder/blob/main/runs/gto-poker-async-duel/output/verification/report.json) | CV verdict + evidence | Knowing what was verified vs. what slipped through |
| [`runs/gto-poker-async-duel/root-cause-analysis.md`](https://github.com/mondrianaire/auto-builder/blob/main/runs/gto-poker-async-duel/root-cause-analysis.md) | RCA — present only if Phase 2 rectification was needed | Understanding any failure-and-recovery path in the build |
| [`runs/gto-poker-async-duel/contracts/`](https://github.com/mondrianaire/auto-builder/tree/main/runs/gto-poker-async-duel/contracts) | Inter-role contracts | API-level expectations across roles |
| [`runs/gto-poker-async-duel/research/`](https://github.com/mondrianaire/auto-builder/tree/main/runs/gto-poker-async-duel/research) | Research-role outputs | What external information the build pulled in |

## Getting oriented in this codebase

Recommended first-session sequence when you open this repo:

1. **Read `README.md`** at the repo root — it contains the build provenance and the original prompt. (For web_app builds, it also points at the live URL.)
2. **List the top-level structure** to get a feel for what's here: source files, tests, build config, dependencies.
3. **Find the entry point.** For web_app deliverables, this is usually `index.html`. For other deliverable kinds, check the README's "What's here" section.
4. **Skim a few representative source files** to understand the project's idioms — naming conventions, where state lives, how modules talk to each other. These are the build's choices and they may or may not match your defaults; defer to existing patterns rather than imposing AutoBuilder conventions or your own.
5. **Try running the deliverable** if there's an obvious way (open `index.html`, run `npm start`, etc.). For web_app builds, opening the live URL is the fastest way to see the actual current state.
6. **Check for tests.** If they exist, run them. If they don't, the build chose not to write any — your call whether to add them as you go.

## Visual iteration capabilities

When you're iterating on a UI-bearing artifact (which web_app deliverables always are, and some other deliverable kinds may be), being able to see the rendered output is a force-multiplier. Two paths are available:

### Path A: Claude in Chrome (recommended for live iteration)

If the user has the [Claude in Chrome](https://www.anthropic.com/news/claude-in-chrome) browser extension installed and authorized for this session, you have access to `mcp__Claude_in_Chrome__*` tools. You can:

- Navigate to the live URL (`https://mondrianaire.github.io/gto-poker-async-duel-AB/` for this build, if it's a web_app)
- Take screenshots of the current state
- Click, type, fill forms, read console output, read network requests
- Iterate on UI changes by viewing the deployed result after each commit

To check whether the Chrome extension is connected, call `mcp__Claude_in_Chrome__list_connected_browsers`. If it returns no browsers, ask the user to install/connect the extension. If they don't want to, fall back to Path B.

### Path B: Headless screenshots via puppeteer (no user setup required)

For automated screenshots without the Chrome extension:

```bash
npm install --save-dev puppeteer
```

Then a minimal screenshot helper (drop into `scripts/screenshot.mjs` or similar):

```javascript
import puppeteer from 'puppeteer';
const url = process.argv[2];
const out = process.argv[3] || 'screenshot.png';
const browser = await puppeteer.launch();
const page = await browser.newPage();
await page.setViewport({ width: 1280, height: 800 });
await page.goto(url, { waitUntil: 'networkidle0' });
await page.screenshot({ path: out, fullPage: true });
await browser.close();
console.log(`Wrote ${out}`);
```

Run with: `node scripts/screenshot.mjs https://example.com/ out.png` and then `Read` the PNG.

For static-site builds you can also screenshot the local file:

```bash
node scripts/screenshot.mjs file:///$(pwd)/index.html local.png
```

Puppeteer bundles its own Chromium so this works in any environment with Node.js, no system browser required.

### Path C: User-driven screenshots (always-works fallback)

When neither A nor B is available, just ask the user to take a screenshot and paste it in. Slower but zero-setup. Useful early in iteration when you and the user are aligning on what changes you're trying to make.

## Product-life mode guidance

A few non-obvious things that flow from "the build factory is sealed and you're on your own":

- **Test before you ship.** There's no automated convergence verifier here. Running the deliverable end-to-end after a change is your responsibility. For web apps: open the live URL or load `index.html` locally and walk through the user flow.
- **Versioning.** This repo started at the promotion commit. Consider adding a `CHANGELOG.md` if you'll be making non-trivial changes — future you (or future agents) will want to know what shifted post-fork.
- **Defer to existing patterns.** The build's code reflects the build's choices. When in doubt, match the existing style/structure rather than rewriting to a different idiom. Refactors that change foundational patterns should be explicit, deliberate, and probably their own commit.
- **Branching.** For non-trivial changes, work on a feature branch and PR back to main — even if you're solo. The PR description is a useful artifact for "what changed and why."
- **Re-promotion safety.** The AutoBuilder workflow that originally created this fork has a product-life-protection guard: if you push any commits authored by your git user (i.e., not `github-actions[bot]`), any future re-promotion attempt is **blocked by default** and requires explicit override. You won't accidentally lose work to upstream meta-construction.

## What's safe to edit

Everything in this repository. AutoBuilder does not write back here; this is the user's home for the product going forward.

The only files that originated from the fork ceremony (rather than from the build itself) are:

- `README.md` at the repo root (auto-generated from `architecture/promoted-repo-readme-template.md` in the corpus)
- `.claude/CLAUDE.md` (this file, auto-generated from `architecture/claude-code-handoff-template.md` in the corpus)

You can edit these freely — they will not be regenerated unless an explicit re-promotion is triggered with the overwrite flag set.

## Session-resume hygiene — stale plan files

Plans written in plan mode are saved to `~/.claude/plans/*.md` and are **never
auto-deleted** — not when the plan is implemented, not when it merges. On every
session resume / context compaction, the harness re-injects whatever plan files
it finds as a system reminder telling the agent to "continue working on it if
not already complete." A finished plan therefore keeps resurfacing on every
future resume — looking exactly like live, unfinished work even when it shipped
days ago. (This is harness behavior, not specific to this repo.)

**When a resume surfaces a plan file, do not assume it is live work.** The
reminder carries no date and no done-flag, so first verify against reality:
compare the plan's contents to the repo and `git log`. If the plan is already
implemented and merged, say so plainly — and **delete the plan file**
(`rm ~/.claude/plans/<name>.md`) — instead of "resuming" it.

**After any plan is merged, delete its plan file** from `~/.claude/plans/` as
part of wrapping up. That is the only durable fix: an undeleted plan file is
guaranteed to haunt the next resume.

---

## Lifecycle vocabulary (if you need it)

For terminology you may encounter while reading the AutoBuilder corpus — Phase 1 / Phase 2 / Three Completion Gates / Fork Ceremony / Three Categories of Run Substrate / Promotion gate model:

- [architecture/build-lifecycle.md](https://github.com/mondrianaire/auto-builder/blob/main/architecture/build-lifecycle.md)
- [architecture/autobuilder-ethos.md](https://github.com/mondrianaire/auto-builder/blob/main/architecture/autobuilder-ethos.md)
- [architecture/file_schemas.md](https://github.com/mondrianaire/auto-builder/blob/main/architecture/file_schemas.md)

These describe the build factory. They do not describe this repository.
